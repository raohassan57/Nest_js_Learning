import { PaymentsInterceptor } from './payments.interceptor';

describe('PaymentsInterceptor', () => {
  it('should be defined', () => {
    expect(new PaymentsInterceptor()).toBeDefined();
  });
});
